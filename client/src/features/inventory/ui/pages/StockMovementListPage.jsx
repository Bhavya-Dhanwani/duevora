import { useQuery } from "@tanstack/react-query";
import { HiOutlineDocumentArrowDown } from "react-icons/hi2";
import { inventoryApi } from "../../api/inventoryApi";
import { Button, DataTable, PageHeader } from "../../../../app/components/common";
import { exportToPdf } from "../../../../lib/exportToPdf";
export default function StockMovementListPage() { const query=useQuery({queryKey:["stockMovements"],queryFn:()=>inventoryApi.listStockMovements()}); const items=query.data?.data||[]; return <div style={{maxWidth:1300,margin:"0 auto"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:16}}><PageHeader title="Stock Movement Audit Trail" subtitle="A read-only history of inventory receipts, issues, transfers, and corrections."/><Button variant="secondary" icon={HiOutlineDocumentArrowDown} onClick={()=>exportToPdf({title:"Stock Movement Audit Trail",filename:"stock_movements",columns:[{key:"date",label:"Date",render:v=>v?new Date(v).toLocaleString():"—"},{key:"product",label:"Product",render:v=>v?.name||"—"},{key:"warehouse",label:"Warehouse",render:v=>v?.name||"—"},{key:"type",label:"Movement"},{key:"quantity",label:"Quantity"},{key:"reference",label:"Reference"}],data:items})}>Export PDF</Button></div><DataTable loading={query.isLoading} data={items} columns={[{key:"date",label:"Date",render:v=>v?new Date(v).toLocaleString():"—"},{key:"product",label:"Product",render:v=>v?.name||"—"},{key:"warehouse",label:"Warehouse",render:v=>v?.name||"—"},{key:"type",label:"Movement"},{key:"quantity",label:"Quantity"},{key:"reference",label:"Reference"}]}/></div>; }